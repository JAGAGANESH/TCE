
AES Encryption  =>  openssl aes-128-cbc -e -in p0.txt -out c0.bin -k "password" -nosalt

AES Decryption  =>  openssl aes-128-cbc -d -in c0.bin -out p1.txt -k "password" –nosalt

Avalance Effect  => AES Decyptions vs AES Decryption

RSA Private Key  =>  openssl genrsa –out pvtkey.pem

RSA Public Key  =>  openssl rsa -pubout -in pvtkey.pem -out pubkey.pem

RSA Private Key in Hexadecimal  =>  openssl rsa -text -in pvtkey.pem

RSA Encryption  =>  openssl rsautl -encrypt -in plain.txt -pubin -inkey pubkey.pem -out c1.bin

RSA Decryption  =>  openssl rsautl -decrypt -in c1.bin -inkey pvtkey.pem -out p2.txt

MD5 Hash  =>  openssl md5 plain.txt

SHA256 Hash  =>  openssl SHA256 plain.txt

Signature using SHA and RSA  =>  openssl dgst -sha1 -sign pvtkey.pem -out s.bin plain.txt

Verify Signature  =>  openssl dgst -sha1 -verify pubkey.pem -signature s.bin plain.txt

